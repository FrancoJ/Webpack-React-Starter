import React from "react";


//Header Component </Header>
const Footer = React.createClass({
  render: function() {
    return (
     <div className="container-fluid">
      <p className="text-center">Footer... because why not.</p>
    
     </div>
    );
  }
});

export default Footer;